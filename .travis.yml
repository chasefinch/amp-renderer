language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
install:
  - pip install -r requirements/test.txt
script:
  if (( ${TRAVIS_PYTHON_VERSION%%.*} >= 3 )); then make; else make test; fi